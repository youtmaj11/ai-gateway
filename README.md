# AI Gateway

A self-hosted, offline-first AI orchestrator gateway built in Rust.

## Getting Started

- Copy `config.toml.example` to `config.toml`
- Update values for your environment
- Run `cargo build`

## Project Structure

See `PROJECT_CONTEXT.md` for exact expected structure.
